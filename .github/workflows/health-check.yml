name: Daily Health Check

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Hit health endpoint
        run: |
          curl -f https://murtaza19-veritify-news-vigilance.hf.space/health || exit 1
